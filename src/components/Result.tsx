import React from 'react';
import './styles/result.css';

const Result = () => {
  const temp = '43'; // todo: replace with context
  return (
    <div className="result">{temp}</div>
  );
};

export default Result;